{
  "name": "critter-feature",
  "version": "0.0.0",
  "private": true,
  "description": "Critter Feature — AI proposes, deterministic engine disposes. (Palastrai project.)",
  "type": "module",
  "scripts": {
    "cli": "tsx src/cli/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "smoke": "tsx src/cli/index.ts bout Tardigrade \"Peregrine Falcon\" --biome Ice --seed 42 --legs 1",
    "game": "tsx scripts/play.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "zod": "^3.23.0"
  }
}
